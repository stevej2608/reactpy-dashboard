from reactpy import use_context
from .settings_store import SettingsContext

# ./solidjs-dashboard/src/settings/dark_mode.ts

# dark_mode, set_dark_mode = use_state(settings.dark_mode)

# def toggle_dark_mode() -> None:
#     settings = use_context(SettingsContext)
#     settings.dark_mode = not settings.dark_mode
#     # _settings = settings.copy(update={'dark_mode' : not dark_mode })
#     # set_settings(_settings)
